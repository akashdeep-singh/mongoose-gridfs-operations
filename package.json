{
  "name": "mongoose-gridfs-operations",
  "version": "0.0.7-alpha.1",
  "description": "Helper functions for reading and writing to gridfs using mongoose.",
  "main": "lib.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "release": "npmversion -i prepatch -p alpha && git push && git push --tags && npm publish"
  },
  "keywords": [
    "mongoose",
    "gridfs"
  ],
  "repository": {
    "type": "git",
    "url": "git://github.com/akashdeep-singh/mongoose-gridfs-operations.git"
  },
  "author": "Akashdeep Singh",
  "license": "MIT",
  "dependencies": {
    "async": "^2.5.0",
    "gridfs-stream": "^1.1.1"
  }
}
